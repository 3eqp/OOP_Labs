#include "stdafx.h"
#include "triangle.h"
#include <iostream> 
using namespace std;

int _tmain(int argc, _TCHAR* argv[]) {
	Triangle A;
	int x = 0, y = 0;
	A.setCoordinates(x, y);
	A.calculations(x1, y1, x2, y2)
		A.print();
	return 0;
}